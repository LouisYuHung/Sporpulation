services:
    backend:
        build:
            context: ./backend
            dockerfile: Dockerfile
        image: sporpulation-backend
        ports:
            - "8080:80"
        environment:
            APP_ENV: local
            APP_DEBUG: "true"
            FRONTEND_URL: http://localhost:5173
            DB_CONNECTION: mysql
            DB_HOST: mysql
            DB_PORT: 3306
            DB_DATABASE: sporpulation
            DB_USERNAME: sporpulation
            DB_PASSWORD: secret
            CACHE_STORE: redis
            QUEUE_CONNECTION: redis
            SESSION_DRIVER: redis
            REDIS_CLIENT: phpredis
            REDIS_HOST: redis
            REDIS_PORT: 6379
        depends_on:
            mysql:
                condition: service_healthy
            redis:
                condition: service_healthy
        volumes:
            - storage_data:/var/www/html/storage

    frontend:
        build:
            context: ./frontend
            dockerfile: Dockerfile
            args:
                # 會在建置時直接打包進 bundle，而 .env 已被 .dockerignore 排除 -
                # 少了這個，建置出來的應用程式就沒有 API base URL，所有呼叫都會退回
                # 打到自己的來源網域。
                VITE_API_BASE_URL: ${VITE_API_BASE_URL:-http://localhost:8080/api}
        image: sporpulation-frontend
        ports:
            - "5173:80"
        depends_on:
            - backend

    mysql:
        image: mysql:8.4
        restart: unless-stopped
        environment:
            MYSQL_DATABASE: sporpulation
            MYSQL_USER: sporpulation
            MYSQL_PASSWORD: secret
            MYSQL_ROOT_PASSWORD: root
        ports:
            - "3306:3306"
        volumes:
            - mysql_data:/var/lib/mysql
        healthcheck:
            test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-u", "root", "-proot"]
            interval: 5s
            timeout: 5s
            retries: 10

    redis:
        image: redis:7-alpine
        restart: unless-stopped
        ports:
            - "6379:6379"
        volumes:
            - redis_data:/data
        healthcheck:
            test: ["CMD", "redis-cli", "ping"]
            interval: 5s
            timeout: 5s
            retries: 10

volumes:
    mysql_data:
    storage_data:
    redis_data:
