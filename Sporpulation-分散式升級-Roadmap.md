# Sporpulation 分散式升級 Roadmap（M1–M6）

> 目的：把現有的「單機正確」版本，升級成「多節點依然正確、而且能證明」的版本。
> 這份作品集的價值不在功能多，而在**你能講清楚：多節點之後哪些假設會壞、哪些其實還成立、你怎麼驗證**。
>
> 使用方式：每個里程碑照著做，做完把 `[ ]` 改成 `[x]`。**做到 M4 就足以支撐「中高級後端」的面試敘事**，M5、M6 是加分。

---

## 北極星：這份作品要證明什麼

你現在的版本已經有一個很強的底子（條件式 UPDATE 佔名額、鎖順序、冪等唯一鍵、fork 併發測試）。但它所有的假設都是**單機**：

- 一台 MySQL
- 鎖只存在單一資料庫實例
- 冪等碼在單一 `idempotency_keys` 資料表
- 併發測試 fork 在同一台機器

分散式版本要回答的核心問題是：**「當我有 N 台 app 節點時，什麼會壞？」**
高手的答案往往反直覺——很多東西**其實不會壞**（因為 MySQL 是唯一真相來源），真正需要額外機制的地方其實很少。能精準指出「哪裡 DB 已經給我原子性、哪裡才真的需要外部協調」，就是資深訊號。

---

## 進度總覽

- [x] **M1** Redis 化冪等與限流
- [x] **M2** 真正的 Queue + Worker（非同步 + 冪等消費者）
- [x] **M3** 想清楚「哪裡才真的需要分散式鎖」（leader election）
- [x] **M4** 水平擴展 + 跨節點壓測 ← **殺手級 README 段落，做到這裡就夠面試用**
- [x] **M5** 可觀測性（讓 p99 是量出來的，不是嘴出來的）
- [ ] **M6（進階選配）** 搶票級架構：Redis 入場閘門 + DB 對帳

---

## M1 — Redis 化冪等與限流

**要教會別人的概念：** 快取層的持久性取捨、限流演算法、「我知道我犧牲了什麼」。

**要改／新增的檔案**
- `docker-compose.yml`：加一個 `redis` service。
- `backend/config/database.php`（或 `cache.php`）：設定 Redis 連線。
- 冪等中介層（你目前寫入 `idempotency_keys` 表的那段邏輯）：抽出一個 `IdempotencyStore` 介面，實作兩種後端 —— `DatabaseIdempotencyStore`（現有）與 `RedisIdempotencyStore`（新增，帶 TTL）。
- 新增限流中介層 `ThrottleRegistration`：套在 `POST /activities/{id}/registration` 上。

**技術選擇**
- 限流演算法用 **token bucket** 或 **sliding window log**（Redis `ZSET` 或 Lua script 做原子遞減）。用 Lua script 保證「檢查 + 遞減」是單一原子操作 —— 這跟你 SQL 那招是同一個精神，剛好呼應。
- TTL 設計：冪等碼 24h、限流視窗依情境（例：單一 user 每分鐘 5 次報名嘗試）。

**這一步的關鍵取捨（README 要正面寫出來）**
你原本 README 就寫過：冪等紀錄故意不放 cache，因為 `cache:clear` 會無聲清掉保護。現在你把它搬到 Redis，等於**主動接受了一個較弱的持久性保證**。所以要講清楚：
- 哪些冪等保護可以接受 Redis（可容忍極少數重放，例如通知去重）；
- 哪些必須留在 DB（絕不能重複佔名額 —— 這層仍由 `unique(activity_id, user_id)` 在 DB 兜底）。
- 結論：**Redis 是第一層過濾（快、可失效），DB 唯一鍵是最終保證（慢、不可失效）。兩層防線，職責不同。**

**README 要放的數字**
- 限流開啟前後：同一 user 100 次/秒的重複請求，被擋下的比例、DB 實際收到的寫入次數（證明限流真的擋在 DB 之前）。
- Redis 冪等命中率：重放請求走 Redis 回放 vs 落到 DB 的比例。

**面試怎麼講（敘事範本）**
> 「冪等我做了兩層。Redis 那層是效能與體驗導向，擋掉大部分重放，但我清楚它的持久性比 DB 弱 —— Redis 掉了保護會無聲消失。所以真正不能出錯的『同一人只佔一個名額』，我沒有信任 Redis，是靠 DB 的唯一鍵兜底。這是刻意的分層：快的那層可以失效，慢的那層不能。」

---

## M2 — 真正的 Queue + Worker

**要教會別人的概念：** at-least-once 投遞、冪等消費者、死信處理、非同步邊界怎麼切。

**要改／新增的檔案**
- `docker-compose.yml`：加一個 `queue-worker` service（跑 `php artisan queue:work`），backend 用 Redis 或改用 database queue driver。
- 新增 Job：例如 `SendRegistrationConfirmation`、`DispatchActivityWebhook`。
- 在報名成功後 `dispatch()` 這些 Job（把「非核心、可延遲」的工作從請求路徑上搬走）。
- 消費者要**冪等**：Job 內部帶一個處理紀錄鍵（例如 `registration_id`），重複投遞不重複發通知。

**技術選擇**
- 先用 Laravel Queue（Redis driver）就好，不用一開始就上 RabbitMQ/Kafka —— 但 README 要說明「若要真正的跨服務事件流，會換成 broker」，展現你知道邊界在哪。
- 設定 retry + backoff，失敗 N 次進 **dead-letter**（`failed_jobs` 表或獨立 queue）。

**關鍵取捨**
- 哪些事**必須同步**（佔名額、寫報名紀錄 —— 影響正確性）、哪些**可以非同步**（通知、對外 webhook、統計）。這條線畫在哪，就是你對「一致性 vs 可用性」的理解。
- at-least-once 代表消費者一定會遇到重複投遞 → 所以消費者必須冪等。這跟你 API 層的冪等是同一個問題在不同層的複現，講出這個連貫性很加分。

**README 要放的數字**
- 加入非同步後，`POST /registration` 的 p50 / p99 延遲下降幅度（把通知搬走後，請求路徑變短）。
- 刻意讓某個 Job 失敗，展示 retry → dead-letter 的完整流程截圖／log。

**面試怎麼講**
> 「我把非核心工作搬到佇列，請求路徑只留下影響正確性的部分。佇列是 at-least-once，所以我的消費者是冪等的 —— 這其實跟我 API 層冪等是同一個問題，只是換到訊息層。失敗的 Job 會 retry，超過上限進死信，不會無聲消失。」

---

## M3 — 想清楚「哪裡才真的需要分散式鎖」

**要教會別人的概念：** 這是最反直覺、也最能展現判斷力的一步。**重點不是加鎖，是證明多數地方不需要鎖。**

**核心洞察（先想通再動手）**
你的 `claimSeat()` 用的條件式 UPDATE（`WHERE joined_count < capacity`）**跨節點本來就正確**，因為：
- MySQL 是唯一真相來源；
- 「檢查 + 佔用」壓成單一原子敘述，鎖只存在該敘述執行期間，由資料庫層仲裁。
- 所以就算有 100 台 app 節點同時打，也不會超賣 —— **你不需要為佔名額加任何分散式鎖。**

那什麼時候才真的需要？→ **只有一種東西該在整個叢集只執行一次的時候。** 例如：
- 你 README 提到的「每小時 `model:prune` 清過期冪等紀錄」。單機時是一個排程；多節點時，每台都會跑排程 → 你需要**確保只有一台真的執行**（leader election / 分散式鎖）。

**要改／新增的檔案**
- 排程指令（清理過期紀錄那支）：包一層分散式鎖。
- Laravel 內建 `Schedule::command(...)->onOneServer()` 底層就是用 cache lock（Redis）—— 可以直接用，但 README 要說明它的原理與陷阱。

**技術選擇 & 陷阱（一定要寫進 README）**
- 單一 Redis 的鎖：簡單，但 Redis 掛了/failover 時鎖可能失效。
- **Redlock**：多 Redis 節點的方案，但學界對它的安全性有爭議（時鐘漂移、GC 暫停下的正確性）。你不用實作 Redlock，但**能講出「為什麼分散式鎖本質上不可能完美、我的場景可以容忍偶爾雙跑因為清理是冪等的」**，比實作它更能證明你懂。

**README 要放的數字／示範**
- 起 2 台節點跑同一個排程，不加鎖 → 展示清理被執行兩次；加 `onOneServer` → 只執行一次。
- 一句關鍵結論：**「叢集裡只有『清理排程』需要協調，佔名額不需要 —— 因為 DB 已經是我的仲裁者。」**

**面試怎麼講（這段是全場最加分的）**
> 「很多人一講分散式就到處加 Redis 鎖，但我盤過之後，我的佔名額路徑其實不需要任何外部鎖 —— MySQL 的條件式 UPDATE 跨節點本來就是原子的，DB 就是仲裁者。整個叢集裡真正需要協調的只有『每小時清理排程』，因為它該全叢集跑一次。我用 leader lock 處理，而且因為清理本身冪等，我可以容忍鎖偶爾失效造成的雙跑 —— 我不需要一個完美的分散式鎖，這讓問題簡單很多。」

---

## M4 — 水平擴展 + 跨節點壓測 ★★★

**這是你的殺手級 README 段落。做到這裡，作品集就足以面「中高級後端」。**

**要教會別人的概念：** 把「單機邏輯驗證」升級成「分散式正確性驗證」。

**要改／新增的檔案**
- `docker-compose.yml`：app 服務 `deploy.replicas: 3`（或手動起 3 個 container）。
- 加 `nginx` 當負載平衡，upstream 指向 3 個 app 節點。
- 改造你的 `activities:check-concurrency` 指令：讓 fork 出來的請求**打進 nginx（LB）而不是單一 app**，這樣才是真的跨節點併發。

**技術選擇**
- LB：nginx（`upstream` + `least_conn` 或 round-robin）。
- 壓測：可沿用你現有的 fork 方式，或補一個 `k6` / `wrk` 腳本產生更高負載並自動輸出 p99。

**README 要放的數字（這幾個數字就是你的作品價值）**
- **零超賣證明**：N 台節點、經過 LB、M 個並行請求搶 K 個名額 → `granted == capacity`、`joined_count == confirmed_rows`、其餘拿到乾淨 409。
- **吞吐**：QPS（每秒成功處理的報名請求數）。
- **延遲**：p50 / p95 / p99。
- **一致性不變式**：在報名/取消交錯的混合負載下，計數器始終等於確認列數（把你現有的三組情境搬到多節點跑一次）。

放一段像這樣的輸出（跨節點版）：
```
CONFIG  nodes=3  lb=nginx  racers=200  capacity=10
PASS    seats granted equals capacity (granted=10 capacity=10)
PASS    losers got clean 409 across all nodes (rejected=190 errors=0)
PASS    counter agrees with confirmed rows (joined_count=10 confirmed_rows=10)
THROUGHPUT  1,840 req/s
LATENCY     p50=41ms  p95=98ms  p99=143ms
```

**面試怎麼講**
> 「我的併發測試一開始是單機 fork，只能驗邏輯。後來我把它升級成三台節點掛負載平衡，讓壓測打進 LB —— 這才是真的跨節點併發。結果超賣依然是零、計數器依然等於確認列數，因為正確性是 DB 保證的，加幾台 app 節點不會破壞它。這一步讓我確認：我的設計的正確性不依賴『只有一台機器』這個假設。」

---

## M5 — 可觀測性

**要教會別人的概念：** 讓 M4 的數字是**量測出來、有圖表**的，不是你手算的。

**要改／新增的檔案**
- 結構化 log（JSON，含 request id / registration id，方便跨節點追蹤）。
- `docker-compose.yml`：加 `prometheus` + `grafana`（可選）。
- 暴露 `/metrics`：報名成功數、409 數、佔名額延遲、佇列積壓長度。

**技術選擇**
- Prometheus client（PHP 有 library，或用 exporter）。
- Grafana 做一張 dashboard 截圖放進 README。

**README 要放的數字**
- 一張 Grafana 截圖：壓測期間的 QPS 曲線、p99 曲線、佇列長度。
- 「開賣瞬間」那一刻的尖峰圖 —— 視覺上非常有說服力。

**面試怎麼講**
> 「我不喜歡口頭宣稱效能，所以我把關鍵指標接到 Prometheus，壓測時的 p99 和佇列積壓都是 Grafana 上量出來的。這也讓我在調限流參數時，有客觀依據而不是憑感覺。」

---

## M6（進階選配）— 搶票級架構：Redis 入場閘門 + DB 對帳

**要教會別人的概念：** 多層一致性 —— 快取層當高速閘門、DB 當真相來源、對帳處理漂移。這是高流量票務系統的經典架構。

**做什麼**
- 在扣 `joined_count` 之前，先在 Redis 用原子 `DECR`（Lua script）做**預扣入場閘門**：只有 Redis 判斷還有名額的請求，才被放行進 MySQL 做最終確認。
- DB 仍是最終真相：Redis 只是「擋掉絕大多數必定失敗的請求」，避免 DB 在開賣瞬間被打爆。
- 寫一個**對帳機制**：定期核對 Redis 預扣數與 DB 實際確認數，處理漂移（例如請求通過閘門但最終 DB 失敗，要把 Redis 名額還回去）。

**技術選擇**
- Redis Lua script 保證預扣原子性。
- 對帳排程（掛在 M3 的 leader lock 底下，全叢集跑一次）。

**關鍵取捨（README 重點）**
- Redis 閘門引入了**兩個真相來源**的風險 —— 這正是你原本 README 裡「為什麼用 counter 而不用 `COUNT(*)`」那段思考的分散式升級版。你要展示：**你知道你引入了漂移風險，並且有對帳機制主動偵測與修正。**
- 誠實邊界：這一層是「削峰」用的，不是「保證正確」用的 —— 正確性仍在 DB。

**README 要放的數字**
- 加閘門前後，DB 在開賣瞬間收到的請求數對比（證明削峰效果）。
- 對帳跑一輪抓到的漂移筆數（證明你不只設計了、還在驗證它）。

**面試怎麼講**
> 「開賣瞬間 DB 會被打爆，所以我在前面加一層 Redis 預扣閘門削峰。但這等於引入第二個真相來源，會漂移 —— 這跟我一開始選 counter 而不用 COUNT(*) 面對的是同一類問題。所以我加了對帳排程主動核對 Redis 和 DB，抓到漂移就修。這一層只負責削峰，正確性我還是留在 DB。」

---

## 什麼時候停？

| 目標 | 建議做到 |
| --- | --- |
| 面「中高級後端」、拉高薪資層級 | **M1–M4**（M4 是必做，其他是鋪陳） |
| 想再強化「我會量測、不是嘴砲」 | 加 **M5** |
| 想直接對標高流量產品公司 | 加 **M6** |

**別為了做完而做完。** 每個里程碑的價值在那段「面試怎麼講」的敘事 —— 如果你做完卻講不出取捨，那就是還沒真的懂，回頭補。作品集打動人的從來不是「我用了 Redis」，而是「我知道我用它換到什麼、又犧牲了什麼」。

---

## 貫穿全程的 README 原則

1. **每個技術決策都配一個「為什麼不用另一個做法」** —— 你現有 README 已經是這個風格（counter vs COUNT(*)、條件式 UPDATE vs SELECT FOR UPDATE），繼續保持，這是你最大的差異化。
2. **每個保護都配一個驗證** —— 你已經有 `check-concurrency`，讓分散式版本每一步都有可重跑的證據。
3. **誠實寫出邊界與缺口** —— 「這個測試環境的侷限是…」「這層只削峰不保證正確」。誠實地標出侷限，比假裝完美更像資深工程師。
